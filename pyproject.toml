[project]
name = "movie-sentiment-analysis"
version = "0.1.0"
description = ""
authors = [
    {name = "pnabilah",email = "puti.nabilah@ui.ac.id"}
]
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "tensorflow (>=2.20.0,<3.0.0)",
    "jupyter (>=1.1.1,<2.0.0)",
    "ipykernel (>=7.2.0,<8.0.0)",
    "matplotlib (>=3.10.8,<4.0.0)",
    "pandas (==2.3.3)",
    "nltk (>=3.10.3,<4.0.0)",
    "wordcloud (>=1.9.6,<2.0.0)",
    "scikit-learn (==1.6.1)",
    "seaborn (==0.13.2)",
    "plotly (==5.24.1)"
]

[tool.poetry]
packages = [{include = "movie_sentiment_analysis", from = "src"}]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
